<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Flight Booking Confirmed - Aerona</title>
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: #333;
      padding: 40px 20px;
      margin: 0;
      min-height: 100vh;
      line-height: 1.6;
    }
    .container {
      width: 100%;
      max-width: 700px;
      margin: 0 auto;
      background-color: white;
      border-radius: 16px;
      overflow: hidden;
      box-shadow: 0 20px 40px rgba(0,0,0,0.15);
      position: relative;
    }
    .container::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 4px;
      background: linear-gradient(90deg, #3498db, #2ecc71, #f39c12);
    }
    .header {
      background: linear-gradient(135deg, #3498db 0%, #2980b9 100%);
      padding: 40px 30px;
      text-align: center;
      color: white;
      position: relative;
    }
    .header h1 {
      margin: 0;
      font-size: 32px;
      font-weight: 700;
      letter-spacing: 1px;
      text-shadow: 0 2px 4px rgba(0,0,0,0.3);
    }
    .flight-icon {
      font-size: 60px;
      margin-bottom: 20px;
      display: block;
    }
    .confirmation-banner {
      background: linear-gradient(135deg, #27ae60 0%, #2ecc71 100%);
      color: white;
      padding: 20px;
      text-align: center;
      font-size: 18px;
      font-weight: 600;
    }
    .body {
      padding: 40px;
      background-color: #f8f9fa;
    }
    .flight-card {
      background: white;
      border-radius: 12px;
      overflow: hidden;
      box-shadow: 0 4px 15px rgba(0,0,0,0.1);
      margin-bottom: 20px;
    }
    .flight-header {
      background: linear-gradient(135deg, #34495e 0%, #2c3e50 100%);
      color: white;
      padding: 20px;
      text-align: center;
    }
    .flight-route {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 20px;
      margin: 20px 0;
    }
    .airport {
      text-align: center;
    }
    .airport-code {
      font-size: 24px;
      font-weight: 700;
    }
    .airport-name {
      font-size: 14px;
      opacity: 0.8;
    }
    .flight-arrow {
      font-size: 20px;
      color: #3498db;
    }
    .flight-details {
      padding: 25px;
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: 20px;
    }
    .detail-item {
      padding: 15px;
      border-left: 4px solid #3498db;
      background: #f8f9fa;
      border-radius: 8px;
    }
    .detail-label {
      font-size: 12px;
      color: #7f8c8d;
      text-transform: uppercase;
      font-weight: 600;
      margin-bottom: 5px;
    }
    .detail-value {
      font-size: 16px;
      color: #2c3e50;
      font-weight: 600;
    }
    .passenger-info {
      background: white;
      border-radius: 12px;
      padding: 25px;
      box-shadow: 0 4px 15px rgba(0,0,0,0.1);
      margin-bottom: 20px;
    }
    .passenger-card {
      background: #f8f9fa;
      border-radius: 8px;
      padding: 15px;
      margin-bottom: 15px;
      border-left: 4px solid #e74c3c;
    }
    .price-summary {
      background: white;
      border-radius: 12px;
      padding: 25px;
      box-shadow: 0 4px 15px rgba(0,0,0,0.1);
      margin-bottom: 30px;
    }
    .price-row {
      display: flex;
      justify-content: space-between;
      padding: 10px 0;
      border-bottom: 1px solid #ecf0f1;
    }
    .price-row:last-child {
      border-bottom: 2px solid #3498db;
      font-weight: 700;
      font-size: 18px;
      color: #2c3e50;
    }
    .important-info {
      background: #fff3cd;
      border: 1px solid #ffeaa7;
      border-radius: 8px;
      padding: 20px;
      margin-bottom: 30px;
    }
    .important-info h3 {
      color: #856404;
      margin: 0 0 15px 0;
    }
    .cta-section {
      text-align: center;
      margin-top: 30px;
    }
    .cta-button {
      display: inline-block;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      padding: 15px 30px;
      text-decoration: none;
      border-radius: 8px;
      font-weight: 600;
      box-shadow: 0 4px 15px rgba(102, 126, 234, 0.3);
      margin: 0 10px 10px 0;
    }
    .footer {
      background: linear-gradient(135deg, #2c3e50 0%, #34495e 100%);
      padding: 25px;
      text-align: center;
      font-size: 14px;
      color: #bdc3c7;
    }
    @media (max-width: 600px) {
      .flight-route {
        flex-direction: column;
        gap: 10px;
      }
      .flight-details {
        grid-template-columns: 1fr;
      }
      .cta-button {
        display: block;
        margin: 10px 0;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <span class="flight-icon">✈️</span>
      <h1>Aerona Travel</h1>
    </div>
    
    <div class="confirmation-banner">
      Flight Booked Successfully! PNR: <%= pnrNumber %>
    </div>
    
    <div class="body">
      <!-- Outbound Flight -->
      <div class="flight-card">
        <div class="flight-header">
          <h3 style="margin: 0;">Outbound Flight</h3>
          <div class="flight-route">
            <div class="airport">
              <div class="airport-code"><%= departureAirportCode %></div>
              <div class="airport-name"><%= departureAirportName %></div>
            </div>
            <div class="flight-arrow">→</div>
            <div class="airport">
              <div class="airport-code"><%= arrivalAirportCode %></div>
              <div class="airport-name"><%= arrivalAirportName %></div>
            </div>
          </div>
        </div>
        
        <div class="flight-details">
          <div class="detail-item">
            <div class="detail-label">Flight Number</div>
            <div class="detail-value"><%= flightNumber %></div>
          </div>
          <div class="detail-item">
            <div class="detail-label">Airline</div>
            <div class="detail-value"><%= airline %></div>
          </div>
          <div class="detail-item">
            <div class="detail-label">Departure</div>
            <div class="detail-value"><%= departureDateTime %></div>
          </div>
          <div class="detail-item">
            <div class="detail-label">Arrival</div>
            <div class="detail-value"><%= arrivalDateTime %></div>
          </div>
          <div class="detail-item">
            <div class="detail-label">Duration</div>
            <div class="detail-value"><%= flightDuration %></div>
          </div>
          <div class="detail-item">
            <div class="detail-label">Class</div>
            <div class="detail-value"><%= travelClass %></div>
          </div>
        </div>
      </div>

      <!-- Return Flight (if applicable) -->
      <% if (returnFlight) { %>
      <div class="flight-card">
        <div class="flight-header">
          <h3 style="margin: 0;">Return Flight</h3>
          <div class="flight-route">
            <div class="airport">
              <div class="airport-code"><%= returnDepartureAirportCode %></div>
              <div class="airport-name"><%= returnDepartureAirportName %></div>
            </div>
            <div class="flight-arrow">→</div>
            <div class="airport">
              <div class="airport-code"><%= returnArrivalAirportCode %></div>
              <div class="airport-name"><%= returnArrivalAirportName %></div>
            </div>
          </div>
        </div>
        
        <div class="flight-details">
          <div class="detail-item">
            <div class="detail-label">Flight Number</div>
            <div class="detail-value"><%= returnFlightNumber %></div>
          </div>
          <div class="detail-item">
            <div class="detail-label">Departure</div>
            <div class="detail-value"><%= returnDepartureDateTime %></div>
          </div>
          <div class="detail-item">
            <div class="detail-label">Arrival</div>
            <div class="detail-value"><%= returnArrivalDateTime %></div>
          </div>
          <div class="detail-item">
            <div class="detail-label">Duration</div>
            <div class="detail-value"><%= returnFlightDuration %></div>
          </div>
        </div>
      </div>
      <% } %>

      <!-- Passenger Information -->
      <div class="passenger-info">
        <h3 style="margin: 0 0 20px 0; color: #2c3e50;">Passenger Information</h3>
        <% passengers.forEach(function(passenger, index) { %>
        <div class="passenger-card">
          <strong>Passenger <%= index + 1 %>:</strong> <%= passenger.name %><br>
          <small>Seat: <%= passenger.seat %> | Meal: <%= passenger.meal %></small>
        </div>
        <% }); %>
      </div>

      <!-- Price Summary -->
      <div class="price-summary">
        <h3 style="margin: 0 0 20px 0; color: #2c3e50;">Price Breakdown</h3>
        <div class="price-row">
          <span>Base Fare</span>
          <span>$<%= baseFare %></span>
        </div>
        <div class="price-row">
          <span>Taxes & Fees</span>
          <span>$<%= taxesAndFees %></span>
        </div>
        <div class="price-row">
          <span>Total Amount</span>
          <span>$<%= totalAmount %></span>
        </div>
      </div>

      <div class="important-info">
        <h3>✈️ Important Travel Information</h3>
        <ul style="margin: 0; padding-left: 20px;">
          <li>Please arrive at the airport at least 2 hours before domestic flights and 3 hours before international flights</li>
          <li>Carry a valid photo ID and your booking confirmation</li>
          <li>Check-in online 24 hours before departure</li>
          <li>Baggage allowance: <%= baggageAllowance %></li>
        </ul>
      </div>

      <div class="cta-section">
        <a href="<%= webCheckInUrl %>" class="cta-button">Web Check-in</a>
        <a href="<%= manageBookingUrl %>" class="cta-button">Manage Booking</a>
        <a href="<%= downloadTicketUrl %>" class="cta-button">Download E-Ticket</a>
      </div>
    </div>
    
    <div class="footer">
      &copy; <%= year %> Aerona Travel. All rights reserved.<br>
      Need help? Contact us at support@aerona.com or call +1-800-AERONA
    </div>
  </div>
</body>
</html>
